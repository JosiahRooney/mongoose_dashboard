<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Goldfish</title>
</head>
<body>
    <% if (goldfish.length > 0) { %>
        <% for ( i in goldfish ) { %>
            <p class="goldfish"><a href="/goldfish/show/<%= goldfish[i]._id %>"><%= goldfish[i] %></a></p>
        <% } %>
    <% } else { %>
        <p>Nothing here</p>
    <% } %>
</body>
</html>